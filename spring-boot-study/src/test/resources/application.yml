spring:
  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
#        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
        show_sql: true

custom:
  clientId: clientApp
  clientSecret: secret
  jwtKey: test
